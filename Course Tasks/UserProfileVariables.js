//A unique identifier assigned to the user
const userID = `B123`;

//The user should be able to update their username
let userName = `Bob123`;

//A permanent record
const accountCreationDate = `2026-03-11`;

//Will change every time the user logs in or out
let currentLoginStatus = true;

//A user's current session
let temporarySessionData = `Session456`;
console.log(`Inside block:`, temporarySessionData);